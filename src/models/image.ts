export class ImageModel {
    constructor(
        creationDate: Date,
        fileName: string,
        height: number,
        id: string,
        latitude: number,
        longitude: number,
        photoURL: string,
        thumbnailURL: string,
        width: number
    ) { }

}